[pytest]
minversion = 7.0
addopts = -q
testpaths = test
python_files = test_*.py
pythonpath = src
; filterwarnings =
;     ignore::DeprecationWarning
markers =
    integration: marks tests as integration / slower tests that exercise the full importer
